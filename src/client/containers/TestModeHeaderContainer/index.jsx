import React from 'react';

function TestModeHeaderContainer({ children }) {
	return <div className="header">{children}</div>;
}

TestModeHeaderContainer.propTypes = {};

export default TestModeHeaderContainer;
